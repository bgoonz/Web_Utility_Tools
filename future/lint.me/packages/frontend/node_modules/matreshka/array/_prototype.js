'use strict';

var _assign;

var assign = require('../_helpers/assign');

var _afterInit = require('./_afterinit');

var mediateItem = require('./mediateitem');

var orderBy = require('./orderby');

var pull = require('./pull');

var recreate = require('./recreate');

var rerender = require('./rerender');

var restore = require('./restore');

var toJSON = require('./tojson');

var pseudoNativeMethods = require('./_pseudonativemethods');

var iterator = require('./iterator');

var symbolIterator = typeof Symbol === 'function' ? Symbol.iterator : '@@iterator';

module.exports = assign((_assign = {
    _afterInit: _afterInit,
    mediateItem: mediateItem,
    orderBy: orderBy,
    pull: pull,
    recreate: recreate,
    rerender: rerender,
    restore: restore,
    toJSON: toJSON,
    length: 0,
    isMatreshkaArray: true
}, _assign[symbolIterator] = iterator, _assign), pseudoNativeMethods);
//# sourceMappingURL=_prototype.js.map