'use strict';

var input = require('./binders/input');

var textarea = require('./binders/textarea');

var select = require('./binders/select');

var progress = require('./binders/progress');

var output = require('./binders/output');

// defaultBinders collection by default contains only one function-checker
module.exports = [function (node) {
    switch (node.tagName) {
        case 'INPUT':
            return input(node.type);
        case 'TEXTAREA':
            return textarea();
        case 'SELECT':
            return select(node.multiple);
        case 'PROGRESS':
            return progress();
        case 'OUTPUT':
            return output();
        default:
            return null;
    }
}];
//# sourceMappingURL=defaultbinders.js.map